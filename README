Testing front end:
Cypress-
        npm start on front and back end
        ./node_modules/.bin/cypress open
        Navigate to api-testing.js or e2e-testing.js

Testing the back end:
npm test (in the routes folder)


to initialize React App:
cd frontend
npx create-react-app frontend
package:
npm install react-router-dom
npm install @auth0/auth0-react
npm install express-jwt


to initialize the Node backend:
cd backend
npm init -y
packages:
npm install --save-dev nodemon
npm install express body-parser concurrently
npm install --save mongoose
npm install @auth0/auth0-react
npm install express-jwt
npm install --save jwks-rsa
npm install react-jwt
npm i express-openid-connect
npm i helmet


npm i dotenv
  env file must be configured so that front end and backend match and link with auth0 
npm install @mui/material @emotion/react @emotion/styled
npm install @mui/material @mui/styled-engine-sc styled-components
npm install @material-ui/icons
run backend with:
npm start
run frontend with:
npm start
Update the frontend/package.json with the proxy:
"proxy": "http://localhost:4000/"

Link to Project Documentation below: 
https://docs.google.com/document/d/19IegGj02M5nfhb-TfTAfuGDEuY08CCN2YN6JQEPUgq0/edit#
